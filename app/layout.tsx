import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const geistSans = Geist({ 
  subsets: ["latin"],
  variable: '--font-geist-sans',
})
const geistMono = Geist_Mono({ 
  subsets: ["latin"],
  variable: '--font-geist-mono',
})

export const metadata: Metadata = {
  title: 'Kitulgala Adventure Carnival | Sri Lanka\'s Premier Adventure Destination',
  description: 'Experience the thrill of white water rafting, waterfall abseiling, jungle trekking, and more at Kitulgala Adventure Carnival - Sri Lanka\'s ultimate adventure tourism destination.',
  keywords: 'Kitulgala, adventure, rafting, Sri Lanka, tourism, waterfall abseiling, jungle trekking, zipline',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
